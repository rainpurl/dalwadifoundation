---
import Page from '../layouts/Page.astro';
import { site } from '../data/site';
---
<Page title={`Support · ${site.name}`}>
  <div class="embed-wrap">
    <div class="embed-inner">
      <iframe
        title="Donation form powered by Zeffy"
        src="https://www.zeffy.com/embed/donation-form/d-lanthropy"
        allowpaymentrequest
        allowtransparency="true"
      ></iframe>
    </div>
  </div>
</Page>

<style is:global>
  /* the donation embed fills the page below the navbar */
  .page:has(.embed-wrap){ padding: 0; min-height: 100svh; }
  .page:has(.embed-wrap) .content{ max-width: none; margin: 0; }
  .embed-wrap{ height: 100svh; padding-top: clamp(64px,9vh,86px); box-sizing: border-box; }
  .embed-inner{ position: relative; height: 100%; width: 100%; overflow: hidden; }
  .embed-inner iframe{ position: absolute; inset: 0; width: 100%; height: 100%; border: 0; }
</style>
